model Ward {
  id       String  @id @default(uuid()) @db.Uuid
  name     String
  code     String  @unique
  city     String
  isActive Boolean @default(true)

  reportedLocations ReportedLocation[]

  @@index([city])
  @@index([isActive])
}
